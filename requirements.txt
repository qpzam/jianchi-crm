# 核心依赖
python-dotenv==1.0.0
requests>=2.28.0
pandas>=1.5.0
openpyxl>=3.0.0
PyPDF2>=3.0.0
pdfplumber>=0.7.0

# AI解析（可选，根据使用的接口安装）
# OpenAI兼容接口
openai>=1.0.0

# Anthropic Claude
anthropic>=0.3.0

# 并发处理
concurrent.futures  # 内置模块

# 数据库
sqlite3  # 内置模块

# 其他工具
tushare-pro>=1.2.0  # 可选，用于获取市场数据